name: check-paths
on:
    push:
        branches:
            - master
        paths: 
            - 'src/*.c'
    pull_request:
        paths: 
            - '.github/workflows/check-paths.yml'
            - 'src/*.c'
jobs:
  job_id:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Hello'
