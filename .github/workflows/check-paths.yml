name: check-paths
on:
  push:
    branches: 
      - main
    paths: 
      - 'src/*.h'
  pull_request:
    paths: 
      - 'src/**'

jobs:
  job_id:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Hello'
