name: check-paths
on:
    pull_request:
        paths: 
#            - '.github/workflows/check-paths.yml'
            - 'src/*.c'
    push:
        branches:
            - master
        paths: 
            - 'src/*.c'
jobs:
  job_id:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Hello'
