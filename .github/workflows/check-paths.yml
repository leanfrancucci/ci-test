name: check-paths
on:
  push:
    branches: 
      - main
  pull_request:
    paths:
      - 'src/foo.c'

jobs:
  job_id:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Hello'
